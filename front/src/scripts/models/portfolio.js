define([
        'jquery',
        'underscore',
        'backbone'
], function(
        $,
        _,
        Backbone
) {
        "use strict";

        var PortfolioModel = Backbone.Model.extend({

        });

        return PortfolioModel;
});
