define([
        'jquery',
        'underscore',
        'backbone'
], function(
        $,
        _,
        Backbone
) {
        "use strict";

        var ImageModel = Backbone.Model.extend({

        });

        return ImageModel;
});
