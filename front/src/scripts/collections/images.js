define([
        'jquery',
        'underscore',
        'backbone'
], function(
        $,
        _,
        Backbone
) {
        "use strict";

        var ImageCollection = Backbone.Collection.extend({

        });

        return ImageCollection;
});
